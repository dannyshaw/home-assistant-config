- id: 'cabinet_dimmer'
  alias: Cabinet Light Dimmer
  trigger:
  - entity_id: sensor.cabinet_dimmer
    platform: state
  condition: []
  action:
  - data_template:
      brightness: '{{ (((state.sensor.cabinet_dimmer | float(0)) * 3250)**0.5) | round(0) }}'
      transition: .25
    service: light.turn_on
    entity_id: light.cabinet_lights
